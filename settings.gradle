pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://maven.emad.dev/repository/maven-public/' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://maven.emad.dev/repository/maven-public/' }
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Jalali DatePicker"
include ':app'
include ':Compose-Jalali-DatePicker'
